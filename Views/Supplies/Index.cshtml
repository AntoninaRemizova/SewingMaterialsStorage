@model IEnumerable<Supply>
@{
    ViewData["Title"] = "Все поступления";
}

<div class="container mt-4">
    <div class="d-flex justify-content-between align-items-center mb-4">
        <h1><i class="bi bi-box-arrow-in-down"></i> @ViewData["Title"]</h1>
        <a asp-action="Create" class="btn btn-primary ms-2">
            <i class="bi bi-plus-circle"></i> Новое поступление
        </a>
    </div>

    <div class="card shadow-sm">
        <div class="card-body p-0">
            <div class="table-responsive">
                <table class="table table-hover mb-0">
                    <thead class="thead-light">
                        <tr>
                            <th>Дата</th>
                            <th>Материал</th>
                            <th>Тип</th>
                            <th class="text-right">Количество</th>
                            <th class="text-right">Сумма</th>
                            <th></th>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach (var item in Model)
                        {
                            <tr>
                                <td>@item.SupplyDate.ToShortDateString()</td>
                                <td>@item.Material?.MaterialName</td>
                                <td>@item.Material?.MaterialType?.TypeName</td>
                                <td class="text-right">@item.Quantity</td>
                                <td class="text-right">@((item.Quantity * item.Material?.PricePerUnit)?.ToString("C"))</td>

                                <!-- Кнопки действий -->
                                <td class="text-end">
                                    <div class="btn-group btn-group-sm"
                                         role="group">

                                        <!-- Кнопка подробностей -->
                                        <a asp-action="Details"
                                           asp-route-id="@item.SupplyId"
                                           class="btn btn-outline-info"
                                           title="Подробности">
                                            <i class="bi bi-info-circle"></i>
                                            Подробнее
                                        </a>

                                        <!-- Кнопка редактирования -->
                                        <a asp-action="Edit"
                                           asp-route-id="@item.SupplyId"
                                           class="btn btn-outline-primary"
                                           title="Редактировать">
                                            <i class="bi bi-pencil"></i>
                                            Изменить
                                        </a>

                                        <!-- Кнопка удаления -->
                                        <a asp-action="Delete"
                                           asp-route-id="@item.SupplyId"
                                           class="btn btn-outline-danger"
                                           title="Удалить">
                                            <i class="bi bi-trash"></i>
                                            Удалить
                                        </a>
                                    </div>
                                </td>
                               
                                
                            </tr>
                        }
                    </tbody>
                </table>
            </div>
        </div>
    </div>
</div>